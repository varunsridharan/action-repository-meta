name: 'Github Repository Metadata'
description: 'Githubction exposes useful metadata as environment variable which can be called / used in a workflow'
author: 'varunsridharan'
branding:
  icon: 'database'
  color: 'yellow'
runs:
  using: 'docker'
  image: 'Dockerfile'
